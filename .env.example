DB_USER=inocmon
DB_PASSWORD=troque_isso_por_algo_forte_sem_caracteres_especiais

INGESTION_DOMAIN=filelogs.cloudy.app.br
TRAEFIK_CERTRESOLVER=leresolver

# Credenciais OAuth do Google Cloud Console (Drive API)
GOOGLE_CLIENT_ID=seu_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=seu_client_secret

# Chave usada para criptografar os refresh_tokens dos tenants no banco.
# Gere UMA VEZ com: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# NUNCA reaproveite a chave abaixo em producao -- ela e so um exemplo.
ENCRYPTION_KEY=FBtkoR3KaBGYRs879rUDiICOByENFwtslVoHqI_yiKc=

# Chave de admin temporaria para proteger o endpoint de busca /v1/tenants/{id}/search
# ate o sistema de login do painel existir. Gere a sua com:
# python3 -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_API_KEY=enMMnkTJvxamofquZC0zR_vA8SGdXxm63Fx2ULehI-w

# Chave usada para assinar os tokens JWT de login dos operadores do painel.
# Gere a sua com: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=6lgziypWHvuYSjCHSlFLo_NEyAGjsExdTEz-e8l3k30
